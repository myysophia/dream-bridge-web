export interface School {
    name: string
    code: string
    logo: string
    region: string
    features: string[]
    doctorPoints: number
    masterPoints: number
    website: string
    phone: string
    email: string
    subjects: string[]
    batch: string
    type: string
} 